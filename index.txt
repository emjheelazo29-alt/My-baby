<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Happy Birthday 💖</title>
  <style>
    body {
      background: linear-gradient(to right, #ffe0f0, #fff0f5);
      font-family: 'Segoe UI', sans-serif;
      text-align: center;
      padding: 40px;
      color: #333;
      animation: fadeIn 2s ease-in;
      position: relative;
      overflow-x: hidden;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    h1 {
      font-size: 2.5em;
      color: #ff6fb5;
      margin-bottom: 20px;
    }

    img {
      max-width: 300px;
      border-radius: 16px;
      box-shadow: 0 8px 20px rgba(255, 111, 181, 0.3);
      margin-bottom: 30px;
      animation: fadeIn 2s ease-in;
    }

    .btn {
      appearance: none;
      border: none;
      cursor: pointer;
      padding: 12px 18px;
      border-radius: 12px;
      font-weight: 700;
      letter-spacing: 0.3px;
      transition: transform 0.1s ease, box-shadow 0.3s ease;
    }

    .btn.primary {
      background: linear-gradient(90deg, #ff6fb5, #ff8fb5);
      color: #fff;
      box-shadow: 0 6px 16px rgba(255, 111, 181, 0.4);
      animation: heartbeat 1.5s infinite;
    }

    @keyframes heartbeat {
      0%   { transform: scale(1); }
      25%  { transform: scale(1.08); }
      40%  { transform: scale(1); }
      60%  { transform: scale(1.08); }
      100% { transform: scale(1); }
    }

    iframe {
      margin-top: 30px;
      border-radius: 12px;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.2);
    }

    .heart {
      position: absolute;
      bottom: -40px;
      left: 50%;
      font-size: 24px;
      color: #ff6fb5;
      animation: floatUp 6s infinite ease-in;
    }

    @keyframes floatUp {
      0% { transform: translateY(0) scale(1); opacity: 1; }
      100% { transform: translateY(-600px) scale(1.5); opacity: 0; }
    }
  </style>
</head>
<body>
  <h1>Happy Birthday, My Luvvv 💖</h1>

  <img src="AUNqhy7hqAcmvVDFZKAYG.jpeg" alt="Our photo together" />

  <button id="musicBtn" class="btn primary">💖 Play our song</button>

  <iframe id="ytplayer" width="560" height="315"
    src="https://www.youtube.com/embed/nvxF-dFNEKo?autoplay=0"
    title="YouTube video player" frameborder="0"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
    allowfullscreen>
  </iframe>

  <!-- Floating hearts -->
  <div class="heart" style="left: 20%;">💖</div>
  <div class="heart" style="left: 40%;">💖</div>
  <div class="heart" style="left: 60%;">💖</div>
  <div class="heart" style="left: 80%;">💖</div>

  <script>
    const audio = document.getElementById('audio');
    const musicBtn = document.getElementById('musicBtn');

    musicBtn.addEventListener('click', async () => {
      try {
        const player = document.getElementById('ytplayer');
        player.scrollIntoView({ behavior: 'smooth' });
      } catch (e) {
        alert("Tap again to allow audio — some browsers need a user gesture.");
      }
    });
  </script>
</body>
</html>
